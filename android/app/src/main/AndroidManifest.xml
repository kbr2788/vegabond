<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

<!--    <uses-feature android:name="android.hardware.type.watch" /> &lt;!&ndash; 카카오맵 &ndash;&gt;-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        >
        <activity android:name=".RivewActivity"></activity>
        <activity android:name=".PlaceActivity" />
        <activity android:name=".map" />
        <activity android:name=".SurveyActivity" />
        <activity android:name=".login.SignupActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" /> <!-- 카카오맵 -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="d52b9640568c331be988bc389de518a7" />
    </application>

</manifest>